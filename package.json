{
  "name": "imagemagick-addon",
  "version": "1.0.0",
  "description": "ImageMagick's Magick++ bindings for NodeJS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-addon-api": "^3.1.0"
  }
}
